/**
 * Implements hook_theme().
 */
function compara_tu_negocio_theme() {
  $theme['compara_tu_negocio_index_theme'] = [
    'variables' => ['from' => NULL, 'to' => NULL],
    'template' => 'index',
  ];

  return $theme;
}
